name: Inputs demo
on: 
  workflow_dispatch:
    inputs:
      environments:
        description: Choose the env
        required: true
        default: dev
  
jobs:
  inputs_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy th APP
        run: echo "Deploying the app to ${{ inputs.environments }} "
  
